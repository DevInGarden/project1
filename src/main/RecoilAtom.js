import { atom } from 'recoil'

export const stname = atom({
    key: 'stname',
    default: null
})

export const endname = atom({
    key: 'endname',
    default: null
})

export const stid = atom({
    key: 'stid',
    default: null
})

export const endid = atom({
    key: 'endid',
    default: null
})

export const inout = atom({
    key: 'inout',
    default: "Login / Join"
})

export const arrt = atom({
    key: 'arrt',
    default: null
})